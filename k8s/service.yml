apiVersion: v1
kind: Service
metadata:
  name: dart-app-service
  labels:
    app: dart-app
spec:
  selector:
    app: dart-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP